# Retire.js CLI as Docker image

## Usage

```bash
$ cd /your/app
$ docker run --rm -v $(pwd):/app fbrandel/retirejs

```

Additional options can be passed to Retire.js. See the [documentation](https://github.com/RetireJS/retire.js/tree/master/node).